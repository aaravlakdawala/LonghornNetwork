import java.util.*;

/**
 * Responsible for forming pods (small groups) of students from a
 * {@link StudentGraph}. The algorithm groups nearby/compatible students into
 * pods of a specified size.
 */
public class PodFormation {
    /**
     * Create a pod formation helper backed by a student graph.
     *
     * @param graph the {@link StudentGraph} used as input for grouping
     */
    public PodFormation(StudentGraph graph) {
        // Constructor
    }

    /**
     * Produce pods of approximately {@code podSize} students each.
     *
     * @param podSize desired pod size (number of students per pod)
     */
    public void formPods(int podSize) {
        // Method signature only
    }
}
